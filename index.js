const name = "Susan";  // Change name to "Susan"
const height = 35;     // Change height to a value less than 40 and greater than 0
const message = `${name} is ${height} inches tall`;

module.exports = {name, height, message};
